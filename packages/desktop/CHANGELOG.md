# Changelog

## [Unreleased]

### Added

- Added a development-only Electron desktop MVP with workspace selection, session list and restore, last-workspace persistence, streaming transcript, model selection, and thinking-level controls.
- Added `pi-desktop.sh`, `pi-desktop.ps1`, `pi-desktop.cmd`, and `npm run desktop` to install missing dependencies, download Electron when needed, build, and launch the app.
